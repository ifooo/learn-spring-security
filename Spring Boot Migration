Spring Boot Migration to 2.x.x


First, we need to update the spring boot version to the latest spring boot version

    <parent>
        <groupId>org.springframework.boot</groupId>
        <artifactId>spring-boot-starter-parent</artifactId>
        <version>2.4.5</version>
        <relativePath /> <!-- lookup parent from repository -->
    </parent>


Update all of the required dependencies to work with the latest spring boot version
 * java to version 11
 * maven plugin to the latest version
 * updated hibernate validation annotations